server {
    listen 80;
    listen [::]:80;
    server_name opentree.cc www.opentree.cc;

    root /var/www/opentree;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # Cache static assets
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 301 redirect www -> non-www
    # Uncomment after SSL is set up:
    # return 301 https://opentree.cc$request_uri;
}

# HTTPS — Uncomment after running certbot
# server {
#     listen 443 ssl http2;
#     server_name opentree.cc www.opentree.cc;
#     ssl_certificate /etc/letsencrypt/live/opentree.cc/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/opentree.cc/privkey.pem;
#     root /var/www/opentree;
#     index index.html;
#     location / { try_files $uri $uri/ /index.html; }
# }
